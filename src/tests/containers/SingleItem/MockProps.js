export const props = {
  currentRevisionPublished: true,
  handleSelectCategory: jest.fn(),
  addNewItems: jest.fn(),
  group:'group',
  rfcId:'rfcId',
  organizationId:'organizationId',
  playerConfig:{},
  loadFilesById: jest.fn(),
  currentUserId: 'currentUserId',
};
