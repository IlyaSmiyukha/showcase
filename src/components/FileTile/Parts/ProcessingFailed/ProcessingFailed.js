import * as React from 'react';
import PropTypes from 'prop-types';

import './ProcessingFailed.less';

const ProcessingFailed = () => {
  return (
    <div className="sc-processing-failed">
      Processing <br /> failed
    </div>
  );
};


export default ProcessingFailed;
